var Cards =(props)=>{
    console.log(props.name)
    
    return(
        <>
        <div>
        <h1>{props.name.name}{props.index.index}</h1>
        </div>
        </>
    )
   
    
    
}

export default Cards